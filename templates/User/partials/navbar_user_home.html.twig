<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="collapse navbar-collapse" id="navbarColor02"> 
        <ul class="navbar-nav ml-auto align-items-center">
            <li class="nav-item">
                <a href="{{path('user_edit_profile', {'id': app.user.id})}}">
                    <img src="{{asset('build/data/avatar.jpg')}}" alt="client_avatar" class="rounded-circle img-fluid d-block" style="width:50px;">
                </a>
            </li>
            <li class="nav-item text-light mx-2">
                <p class="m-0 text-dark">{{app.user.firstname}} {{app.user.lastname}}</p>
                {% if app.user.ispremium %}
                    <span class="badge badge-pill badge-success">Premium</span>
                {% else %}
                    <span class="badge badge-pill badge-warning">Basique</span>
                {% endif %}
            </li>
            <li class="nav-item">
                <a href="{{path('account_logout')}}" class="btn action_button text-light nav-link">Déconnexion</a>
            </li>
        </ul>
    </div>
</nav>

